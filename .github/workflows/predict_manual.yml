name: Generate Weekly Football Predictions (Manual fresh)

on:
  workflow_dispatch:  # guarantees "Run workflow" button

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    env:
      PYTHONFAULTHANDLER: "1"
      PYTHONUNBUFFERED: "1"

    steps:
      - name: 1) Checkout repo
        uses: actions/checkout@v4

      - name: 2) Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 3) Install dependencies (pinned core + project)
        run: |
          python -m pip install --upgrade pip
          pip install "pandas==2.2.2" "numpy==1.26.4" "pyarrow==16.1.0" "requests>=2.31.0" "scikit-learn>=1.4.0" "joblib>=1.3.0"
          pip install -r requirements.txt || true

      - name: 4) Guard: raw CSVs must exist in repo
        run: |
          ls -lh data/raw/cfbd || true
          if [ ! -f "data/raw/cfbd/cfb_schedule.csv" ]; then
            echo "No raw CFBD data found at data/raw/cfbd/. Run 'Manual Fetch and Commit CFBD Data' first."
            exit 1
          fi

      - name: 5) Build training dataset
        run: python -m scripts.build_dataset

      - name: 6) Train model
        run: python -m scripts.train_model

      - name: 7) Generate predictions
        env:
          CFBD_API_KEY: ${{ secrets.CFBD_API_KEY }}  # only needed if predict hits API
        run: python -m scripts.predict

      - name: 8) Create website snapshot
        run: python -m scripts.generate_data_snapshot

      - name: 9) Commit & push results
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          branch: main
          commit_message: "📊 Update weekly predictions & data snapshot"
          file_pattern: |
            docs/data/predictions.json
            docs/data/data_snapshot.json
          commit_user_name: "GitHub Actions Bot"
          commit_user_email: "github-actions@github.com"
          commit_author: "GitHub Actions Bot <github-actions@github.com>"
