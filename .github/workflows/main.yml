# .github/workflows/main.yml (FINAL PRODUCTION VERSION)

name: Generate Weekly Football Predictions

on:
  workflow_dispatch:
  schedule:
    - cron: '0 12 * * 2'
  push:
    paths:
      - 'docs/input/games.txt'

jobs:
  run-prediction-pipeline:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 1. Checkout Repository
        uses: actions/checkout@v4

      - name: 2. Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 3. Install Dependencies
        run: pip install -r requirements.txt

      - name: 4. Cache and Restore Raw Data
        id: cache-raw-data
        uses: actions/cache@v4
        with:
          path: data/raw/cfbd
          key: ${{ runner.os }}-raw-cfbd-data-v1

      - name: 5. Fetch Latest Data from API (if cache missed)
        # This step now ONLY runs if the cache was not found in the previous step.
        if: steps.cache-raw-data.outputs.cache-hit != 'true'
        env:
          CFBD_API_KEY: ${{ secrets.CFBD_API_KEY }}
        run: python -m scripts.fetch_cfbd

      - name: 6. Build Training Dataset
        run: python -m scripts.build_dataset

      - name: 7. Train Prediction Model
        run: python -m scripts.train_model

      - name: 8. Generate New Predictions
        env:
          CFBD_API_KEY: ${{ secrets.CFBD_API_KEY }}
        run: python -m scripts.predict

      - name: 9. Generate Data Snapshot for Website
        run: python -m scripts.generate_data_snapshot

      - name: 10. Commit and Push Results
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          branch: main
          commit_message: "📊 Update weekly predictions & data snapshot"
          file_pattern: "docs/data/predictions.json docs/data/data_snapshot.json"
          commit_user_name: "GitHub Actions Bot"
          commit_user_email: "github-actions@github.com"
          commit_author: "GitHub Actions Bot <github-actions@github.com>"

